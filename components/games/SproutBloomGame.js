// components/games/SproutBloomGame.js
// Thin wrapper — re-exports the TypeScript App from the SproutBloom/ subfolder.
// The App accepts optional { studentData, updateStudentData, showToast } props
// which are passed through automatically by GamesTab and StudentGames.
export { default } from './SproutBloom/App';

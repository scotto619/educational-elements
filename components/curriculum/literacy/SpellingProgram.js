import React from 'react';
import SpellingCenter from '../../literacy/SpellingCenter';

const SpellingProgram = ({ showToast }) => {
  return <SpellingCenter showToast={showToast} />;
};

export default SpellingProgram;
